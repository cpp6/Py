class Node:
    def __init__(self, data):
        self.data = data
        self.next = None


class CircularSinglyLinkedList:
    def __init__(self):
        self.head = None

    def insert_end(self, data):
        new_node = Node(data)
        if self.head is None:
            self.head = new_node
            new_node.next = self.head
        else:
            temp = self.head
            while temp.next != self.head:
                temp = temp.next
            temp.next = new_node
            new_node.next = self.head

    def insert_at_position(self, data, position):
        if position <= 0:
            print("Invalid position. Position must be a positive integer.")
            return

        new_node = Node(data)

        if self.head is None:
            if position == 1:
                self.head = new_node
                new_node.next = self.head
            else:
                print("List is empty. Can only insert at position 1.")
            return

        if position == 1:
            new_node.next = self.head
            temp = self.head
            while temp.next != self.head:
                temp = temp.next
            temp.next = new_node
            self.head = new_node
            return

        temp = self.head
        count = 1
        while count < position - 1 and temp.next != self.head:
            temp = temp.next
            count += 1

        if count < position - 1:
            print(f"Position {position} is beyond the list length. Inserting at the end.")
            new_node.next = self.head
            temp.next = new_node
            return

        new_node.next = temp.next
        temp.next = new_node

    def display(self):
        if self.head is None:
            print("List is empty")
            return

        temp = self.head
        while True:
            print(temp.data, end=" -> ")
            temp = temp.next
            if temp == self.head:
                break
        print("(head)")


cll = CircularSinglyLinkedList()

while True:
    print("\n1.Insert at End\n2.Insert Between\n3.Display\n0.Exit")
    ch = int(input("Enter choice: "))

    if ch == 1:
        d = int(input("Enter data: "))
        cll.insert_end(d)

    elif ch == 2:
        d = int(input("Enter data: "))
        pos = int(input("Enter position: "))
        cll.insert_at_position(d, pos)

    elif ch == 3:
        cll.display()

    elif ch == 0:
        exit(0)

    else:
        print("Invalid choice")


Output:
1.	Insert at End
2.	Insert Between
3.	Display
0.	Exit
Enter choice: 1
Enter data: 11

1.	Insert at End
2.	Insert Between
3.	Display
0.	Exit
 
Enter choice: 1
Enter data: 21

1.	Insert at End
2.	Insert Between
3.	Display
0.	Exit
Enter choice: 1
Enter data: 45

1.	Insert at End
2.	Insert Between
3.	Display
0.	Exit
Enter choice: 1
Enter data: 67
1.	Insert at End
2.	Insert Between
3.	Display
0.	Exit
Enter choice: 1
Enter data: 55

1.	Insert at End
2.	Insert Between
3.	Display
0.	Exit
Enter choice: 3
11 -> 21 -> 45 -> 67 -> 55 -> (head)

1.	Insert at End
2.	Insert Between
3.	Display
0.	Exit
Enter choice: 2
Enter data: 90
Enter position: 3
 
1.	Insert at End
2.	Insert Between
3.	Display
0.	Exit
Enter choice: 3
11 -> 21 -> 90 -> 45 -> 67 -> 55 -> (head)

1.	Insert at End
2.	Insert Between
3.	Display
0.	Exit
Enter
